{
  "//": [
    "Type-checks the Node half including its tests. tsconfig.node.json excludes the tests",
    "because they must not be emitted to dist/, but they are the part most likely to notice a",
    "contract changing, so they are checked here.",
    "",
    "The dashboard is checked separately by `vue-tsc -p tsconfig.ui.json`, which is what",
    "understands .vue files; plain tsc cannot resolve them."
  ],
  "extends": "./tsconfig.node.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": []
}
